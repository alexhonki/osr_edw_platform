{
	"role":{
		"name": "osr.sec.edw.platform.asic.dac::read_g#",
    	"schema_privileges":[ 
    	{ 
        	"reference":"EDW_ASIC_G", 
        	"privileges_with_grant_option":[ "SELECT", "SELECT CDS METADATA", "EXECUTE" ]
    	}
    	] 
    }
}