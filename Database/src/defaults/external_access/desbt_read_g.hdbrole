{
	"role":{
		"name": "osr.sec.edw.platform.desbt.dac::read_g#",
    	"schema_privileges":[ 
    	{ 
        	"reference":"EDW_DESBT_G", 
        	"privileges_with_grant_option":[ "SELECT", "SELECT CDS METADATA", "EXECUTE" ]
        	
    	}
    	] 
    }
}