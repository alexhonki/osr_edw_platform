{
	"role":{
		"name": "osr.sec.edw.platform.rms.dac::read",
    	"schema_privileges":[ 
    	{ 
        	"reference":"EDW_RMS", 
        	"privileges":[ "SELECT", "SELECT CDS METADATA", "EXECUTE"]
        	
    	}
    	] 
    }
}