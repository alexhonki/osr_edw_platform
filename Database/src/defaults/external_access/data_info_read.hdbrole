{
	"role":{
		"name": "osr.sec.edw.platform.data.info.dac::read",
    	"schema_privileges":[ 
    	{ 
        	"reference":"EDW_DATA_INFO", 
        	"privileges":[ "SELECT", "SELECT CDS METADATA", "EXECUTE"]
        	
    	}
    	] 
    }
}