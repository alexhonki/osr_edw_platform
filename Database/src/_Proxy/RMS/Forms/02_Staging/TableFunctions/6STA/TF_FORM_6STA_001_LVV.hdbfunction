FUNCTION "osr.edw.staging.rms.forms.tf.proxy::TF_FORM_6STA_001_LVV" ( )
       RETURNS TABLE (
    	CLIENT		NVARCHAR(3),
        OBJECT_ID	NVARCHAR(32),
        VERSION 	NVARCHAR(5)
       )
       LANGUAGE SQLSCRIPT 
       SQL SECURITY INVOKER AS 
BEGIN 

RETURN
SELECT 
CLIENT,
OBJECT_ID,
LPAD(MAX(TO_INT(VERSION)),5,'0') AS VERSION
FROM 
"osr.edw.staging.td.rms.synonym::CDS_TRM.DSO.Z1006STA00000000.active_data"
GROUP BY CLIENT, OBJECT_ID
;

END;