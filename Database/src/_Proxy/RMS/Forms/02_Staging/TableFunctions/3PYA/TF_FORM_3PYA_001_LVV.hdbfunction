FUNCTION "osr.edw.staging.rms.forms.tf.proxy::TF_FORM_3PYA_001_LVV" ( )
RETURNS TABLE (
OBJECT_ID	NVARCHAR(32),
VERSION 	NVARCHAR(5)
)
LANGUAGE SQLSCRIPT 
SQL SECURITY INVOKER AS 
BEGIN 

RETURN
SELECT
OBJECT_ID,
MAX(VERSION) AS VERSION
FROM 
"osr.edw.staging.td.rms.synonym::CDS_TRM.DSO.Z1003PYA00000000.active_data"
GROUP BY OBJECT_ID
;

END;